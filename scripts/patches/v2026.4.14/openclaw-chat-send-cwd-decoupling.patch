diff --git a/src/agents/pi-embedded-runner/run/attempt.ts b/src/agents/pi-embedded-runner/run/attempt.ts
index 38aca02..3694c45 100644
--- a/src/agents/pi-embedded-runner/run/attempt.ts
+++ b/src/agents/pi-embedded-runner/run/attempt.ts
@@ -360,6 +360,7 @@ export async function runEmbeddedAttempt(
   params: EmbeddedRunAttemptParams,
 ): Promise<EmbeddedRunAttemptResult> {
   const resolvedWorkspace = resolveUserPath(params.workspaceDir);
+  const resolvedCwd = resolveUserPath(params.cwd ?? params.workspaceDir);
   const runAbortController = new AbortController();
   // Proxy bootstrap must happen before timeout tuning so the timeouts wrap the
   // active EnvHttpProxyAgent instead of being replaced by a bare proxy dispatcher.
@@ -371,19 +372,22 @@ export async function runEmbeddedAttempt(
   );
 
   await fs.mkdir(resolvedWorkspace, { recursive: true });
+  await fs.mkdir(resolvedCwd, { recursive: true });
 
   const sandboxSessionKey = params.sessionKey?.trim() || params.sessionId;
   const sandbox = await resolveSandboxContext({
     config: params.config,
     sessionKey: sandboxSessionKey,
-    workspaceDir: resolvedWorkspace,
+    workspaceDir: resolvedCwd,
   });
-  const effectiveWorkspace = sandbox?.enabled
+  const effectiveWorkspace = resolvedWorkspace;
+  const effectiveCwd = sandbox?.enabled
     ? sandbox.workspaceAccess === "rw"
-      ? resolvedWorkspace
+      ? resolvedCwd
       : sandbox.workspaceDir
-    : resolvedWorkspace;
+    : resolvedCwd;
   await fs.mkdir(effectiveWorkspace, { recursive: true });
+  await fs.mkdir(effectiveCwd, { recursive: true });
   const { sessionAgentId } = resolveSessionAgentIds({
     sessionKey: params.sessionKey,
     config: params.config,
@@ -521,12 +525,12 @@ export async function runEmbeddedAttempt(
             sessionId: params.sessionId,
             runId: params.runId,
             agentDir,
-            workspaceDir: effectiveWorkspace,
-            // When sandboxing uses a copied workspace (`ro` or `none`), effectiveWorkspace points
-            // at the sandbox copy. Spawned subagents should inherit the real workspace instead.
+            workspaceDir: effectiveCwd,
+            // When sandboxing uses a copied workspace (`ro` or `none`), effectiveCwd points
+            // at the sandbox copy. Spawned subagents should inherit the real cwd instead.
             spawnWorkspaceDir: resolveAttemptSpawnWorkspaceDir({
               sandbox,
-              resolvedWorkspace,
+              resolvedWorkspace: resolvedCwd,
             }),
             config: params.config,
             abortSignal: runAbortController.signal,
@@ -564,7 +568,7 @@ export async function runEmbeddedAttempt(
       tools: toolsEnabled ? toolsRaw : [],
       provider: params.provider,
       config: params.config,
-      workspaceDir: effectiveWorkspace,
+      workspaceDir: effectiveCwd,
       env: process.env,
       modelId: params.modelId,
       modelApi: params.model.api,
@@ -575,7 +579,7 @@ export async function runEmbeddedAttempt(
       ? await getOrCreateSessionMcpRuntime({
           sessionId: params.sessionId,
           sessionKey: params.sessionKey,
-          workspaceDir: effectiveWorkspace,
+          workspaceDir: effectiveCwd,
           cfg: params.config,
         })
       : undefined;
@@ -590,7 +594,7 @@ export async function runEmbeddedAttempt(
       : undefined;
     const bundleLspRuntime = toolsEnabled
       ? await createBundleLspToolRuntime({
-          workspaceDir: effectiveWorkspace,
+          workspaceDir: effectiveCwd,
           cfg: params.config,
           reservedToolNames: [
             ...tools.map((tool) => tool.name),
@@ -612,7 +616,7 @@ export async function runEmbeddedAttempt(
       tools: effectiveTools,
       provider: params.provider,
       config: params.config,
-      workspaceDir: effectiveWorkspace,
+      workspaceDir: effectiveCwd,
       env: process.env,
       modelId: params.modelId,
       modelApi: params.model.api,
@@ -661,7 +665,7 @@ export async function runEmbeddedAttempt(
     const sandboxInfo = buildEmbeddedSandboxInfo(sandbox, params.bashElevated);
     const reasoningTagHint = isReasoningTagProvider(params.provider, {
       config: params.config,
-      workspaceDir: effectiveWorkspace,
+      workspaceDir: effectiveCwd,
       env: process.env,
       modelId: params.modelId,
       modelApi: params.model.api,
@@ -702,7 +706,7 @@ export async function runEmbeddedAttempt(
       config: params.config,
       agentId: sessionAgentId,
       workspaceDir: effectiveWorkspace,
-      cwd: effectiveWorkspace,
+      cwd: effectiveCwd,
       runtime: {
         host: machineName,
         os: `${os.type()} ${os.release()}`,
@@ -723,9 +727,9 @@ export async function runEmbeddedAttempt(
     const effectivePromptMode = params.toolsAllow?.length ? ("minimal" as const) : promptMode;
     const effectiveSkillsPrompt = params.toolsAllow?.length ? undefined : skillsPrompt;
     const docsPath = await resolveOpenClawDocsPath({
-      workspaceDir: effectiveWorkspace,
+      workspaceDir: effectiveCwd,
       argv1: process.argv[1],
-      cwd: effectiveWorkspace,
+      cwd: effectiveCwd,
       moduleUrl: import.meta.url,
     });
     const ttsHint = params.config ? buildTtsSystemPromptHint(params.config) : undefined;
@@ -746,11 +750,11 @@ export async function runEmbeddedAttempt(
     const promptContribution = resolveProviderSystemPromptContribution({
       provider: params.provider,
       config: params.config,
-      workspaceDir: effectiveWorkspace,
+      workspaceDir: effectiveCwd,
       context: {
         config: params.config,
         agentDir: params.agentDir,
-        workspaceDir: effectiveWorkspace,
+        workspaceDir: effectiveCwd,
         provider: params.provider,
         modelId: params.modelId,
         promptMode: effectivePromptMode,
@@ -766,7 +770,7 @@ export async function runEmbeddedAttempt(
         agentId: sessionAgentId,
       }) ??
       buildEmbeddedSystemPrompt({
-        workspaceDir: effectiveWorkspace,
+        workspaceDir: effectiveCwd,
         defaultThinkLevel: params.thinkLevel,
         reasoningLevel: params.reasoningLevel ?? "off",
         extraSystemPrompt: params.extraSystemPrompt,
@@ -798,11 +802,11 @@ export async function runEmbeddedAttempt(
     const appendPrompt = transformProviderSystemPrompt({
       provider: params.provider,
       config: params.config,
-      workspaceDir: effectiveWorkspace,
+      workspaceDir: effectiveCwd,
       context: {
         config: params.config,
         agentDir: params.agentDir,
-        workspaceDir: effectiveWorkspace,
+        workspaceDir: effectiveCwd,
         provider: params.provider,
         modelId: params.modelId,
         promptMode: effectivePromptMode,
@@ -819,7 +823,7 @@ export async function runEmbeddedAttempt(
       sessionKey: params.sessionKey,
       provider: params.provider,
       model: params.modelId,
-      workspaceDir: effectiveWorkspace,
+      workspaceDir: effectiveCwd,
       bootstrapMaxChars,
       bootstrapTotalMaxChars,
       bootstrapTruncation: buildBootstrapTruncationReportMeta({
@@ -861,7 +865,7 @@ export async function runEmbeddedAttempt(
         provider: params.provider,
         modelId: params.modelId,
         config: params.config,
-        workspaceDir: effectiveWorkspace,
+        workspaceDir: effectiveCwd,
         env: process.env,
         model: params.model,
       });
@@ -906,11 +910,11 @@ export async function runEmbeddedAttempt(
         sessionFile: params.sessionFile,
         hadSessionFile,
         sessionId: params.sessionId,
-        cwd: effectiveWorkspace,
+        cwd: effectiveCwd,
       });
 
       const settingsManager = createPreparedEmbeddedPiSettingsManager({
-        cwd: effectiveWorkspace,
+        cwd: effectiveCwd,
         agentDir,
         cfg: params.config,
       });
@@ -933,7 +937,7 @@ export async function runEmbeddedAttempt(
       let resourceLoader: DefaultResourceLoader | undefined;
       if (extensionFactories.length > 0) {
         resourceLoader = new DefaultResourceLoader({
-          cwd: resolvedWorkspace,
+          cwd: resolvedCwd,
           agentDir,
           settingsManager,
           extensionFactories,
@@ -974,7 +978,7 @@ export async function runEmbeddedAttempt(
       const allCustomTools = [...customTools, ...clientToolDefs];
 
       ({ session } = await createAgentSession({
-        cwd: resolvedWorkspace,
+        cwd: resolvedCwd,
         agentDir,
         authStorage: params.authStorage,
         modelRegistry: params.modelRegistry,
@@ -1051,7 +1055,7 @@ export async function runEmbeddedAttempt(
         model: params.model,
         cfg: params.config,
         agentDir,
-        workspaceDir: effectiveWorkspace,
+        workspaceDir: effectiveCwd,
       });
       const shouldUseWebSocketTransport = shouldUseOpenAIWebSocketTransport({
         provider: params.provider,
@@ -1090,7 +1094,7 @@ export async function runEmbeddedAttempt(
       const providerTextTransforms = resolveProviderTextTransforms({
         provider: params.provider,
         config: params.config,
-        workspaceDir: effectiveWorkspace,
+        workspaceDir: effectiveCwd,
       });
       if (providerTextTransforms) {
         activeSession.agent.streamFn = wrapStreamFnTextTransforms({
@@ -1112,7 +1116,7 @@ export async function runEmbeddedAttempt(
         },
         params.thinkLevel,
         sessionAgentId,
-        effectiveWorkspace,
+        effectiveCwd,
         params.model,
         agentDir,
       );
@@ -1323,7 +1327,7 @@ export async function runEmbeddedAttempt(
           provider: params.provider,
           allowedToolNames,
           config: params.config,
-          workspaceDir: effectiveWorkspace,
+          workspaceDir: effectiveCwd,
           env: process.env,
           model: params.model,
           sessionManager,
@@ -1337,7 +1341,7 @@ export async function runEmbeddedAttempt(
           modelId: params.modelId,
           provider: params.provider,
           config: params.config,
-          workspaceDir: effectiveWorkspace,
+          workspaceDir: effectiveCwd,
           env: process.env,
           model: params.model,
           sessionId: params.sessionId,
@@ -1847,7 +1851,7 @@ export async function runEmbeddedAttempt(
           // Images are prompt-local only (pi-like behavior).
           const imageResult = await detectAndLoadPromptImages({
             prompt: effectivePrompt,
-            workspaceDir: effectiveWorkspace,
+            workspaceDir: effectiveCwd,
             model: params.model,
             existingImages: params.images,
             imageOrder: params.imageOrder,
diff --git a/src/agents/pi-embedded-runner/run/params.ts b/src/agents/pi-embedded-runner/run/params.ts
index 74d6839..4430527 100644
--- a/src/agents/pi-embedded-runner/run/params.ts
+++ b/src/agents/pi-embedded-runner/run/params.ts
@@ -66,6 +66,7 @@ export type RunEmbeddedPiAgentParams = {
   allowGatewaySubagentBinding?: boolean;
   sessionFile: string;
   workspaceDir: string;
+  cwd?: string;
   agentDir?: string;
   config?: OpenClawConfig;
   skillsSnapshot?: SkillSnapshot;
diff --git a/src/agents/pi-embedded-runner/run.ts b/src/agents/pi-embedded-runner/run.ts
index 8f0398e..9a0e984 100644
--- a/src/agents/pi-embedded-runner/run.ts
+++ b/src/agents/pi-embedded-runner/run.ts
@@ -693,6 +693,7 @@ export async function runEmbeddedPiAgent(
             hasRepliedRef: params.hasRepliedRef,
             sessionFile: params.sessionFile,
             workspaceDir: resolvedWorkspace,
+            cwd: params.cwd,
             agentDir,
             config: params.config,
             allowGatewaySubagentBinding: params.allowGatewaySubagentBinding,
diff --git a/src/auto-reply/get-reply-options.types.ts b/src/auto-reply/get-reply-options.types.ts
index 82af92d..389d8c7 100644
--- a/src/auto-reply/get-reply-options.types.ts
+++ b/src/auto-reply/get-reply-options.types.ts
@@ -32,6 +32,8 @@ export type ReplyThreadingPolicy = {
 export type GetReplyOptions = {
   /** Override run id for agent events (defaults to random UUID). */
   runId?: string;
+  /** Optional runtime cwd for file tools and shell commands. */
+  cwd?: string;
   /** Abort signal for the underlying agent run. */
   abortSignal?: AbortSignal;
   /** Optional inbound images (used for webchat attachments). */
diff --git a/src/auto-reply/reply/agent-runner-utils.ts b/src/auto-reply/reply/agent-runner-utils.ts
index 5697ec7..3389ab3 100644
--- a/src/auto-reply/reply/agent-runner-utils.ts
+++ b/src/auto-reply/reply/agent-runner-utils.ts
@@ -159,6 +159,7 @@ export function buildEmbeddedRunBaseParams(params: {
   return {
     sessionFile: params.run.sessionFile,
     workspaceDir: params.run.workspaceDir,
+    cwd: params.run.cwd,
     agentDir: params.run.agentDir,
     config,
     skillsSnapshot: params.run.skillsSnapshot,
diff --git a/src/auto-reply/reply/get-reply-run.ts b/src/auto-reply/reply/get-reply-run.ts
index 3a0b044..a0ab223 100644
--- a/src/auto-reply/reply/get-reply-run.ts
+++ b/src/auto-reply/reply/get-reply-run.ts
@@ -183,6 +183,7 @@ type RunPreparedReplyParams = {
   sessionId?: string;
   storePath?: string;
   workspaceDir: string;
+  cwd?: string;
   abortedLastRun: boolean;
 };
 
@@ -223,6 +224,7 @@ export async function runPreparedReply(
     sessionId,
     storePath,
     workspaceDir,
+    cwd,
     sessionStore,
   } = params;
   let {
@@ -644,6 +646,7 @@ export async function runPreparedReply(
         (ctx.GatewayClientScopes ?? []).includes("operator.admin"),
       sessionFile: preparedSessionState.sessionFile,
       workspaceDir,
+      cwd,
       config: cfg,
       skillsSnapshot,
       provider,
diff --git a/src/auto-reply/reply/get-reply.ts b/src/auto-reply/reply/get-reply.ts
index 295ec03..616b2a4 100644
--- a/src/auto-reply/reply/get-reply.ts
+++ b/src/auto-reply/reply/get-reply.ts
@@ -215,6 +215,9 @@ export async function getReplyFromConfig(
         ensureBootstrapFiles: !agentCfg?.skipBootstrap && !isFastTestEnv,
       });
   const workspaceDir = workspace.dir;
+  const runCwd =
+    normalizeOptionalString(resolvedOpts?.cwd) ??
+    normalizeOptionalString(agentCfg?.cwd);
   const agentDir = resolveAgentDir(cfg, agentId);
   const timeoutMs = resolveAgentTimeoutMs({ cfg, overrideSeconds: opts?.timeoutOverrideSeconds });
   const configuredTypingSeconds =
@@ -414,6 +417,7 @@ export async function getReplyFromConfig(
       sessionId,
       storePath,
       workspaceDir,
+      cwd: runCwd,
       abortedLastRun,
     });
   }
@@ -632,6 +636,7 @@ export async function getReplyFromConfig(
     sessionId,
     storePath,
     workspaceDir,
+    cwd: runCwd,
     abortedLastRun,
   });
 }
diff --git a/src/auto-reply/reply/queue/types.ts b/src/auto-reply/reply/queue/types.ts
index d760fee..e6c9e09 100644
--- a/src/auto-reply/reply/queue/types.ts
+++ b/src/auto-reply/reply/queue/types.ts
@@ -60,6 +60,7 @@ export type FollowupRun = {
     traceAuthorized?: boolean;
     sessionFile: string;
     workspaceDir: string;
+    cwd?: string;
     config: OpenClawConfig;
     skillsSnapshot?: SkillSnapshot;
     provider: string;
diff --git a/src/config/types.agent-defaults.ts b/src/config/types.agent-defaults.ts
index 132759f..37b2883 100644
--- a/src/config/types.agent-defaults.ts
+++ b/src/config/types.agent-defaults.ts
@@ -178,6 +178,8 @@ export type AgentDefaultsConfig = {
   models?: Record<string, AgentModelEntryConfig>;
   /** Agent working directory (preferred). Used as the default cwd for agent runs. */
   workspace?: string;
+  /** Optional runtime cwd for file tools and shell commands. */
+  cwd?: string;
   /** Optional default allowlist of skills for agents that do not set agents.list[].skills. */
   skills?: string[];
   /** Optional repository root for system prompt runtime line (overrides auto-detect). */
diff --git a/src/config/types.agents.ts b/src/config/types.agents.ts
index 59e6b6d..40f50e3 100644
--- a/src/config/types.agents.ts
+++ b/src/config/types.agents.ts
@@ -67,6 +67,7 @@ export type AgentConfig = {
   default?: boolean;
   name?: string;
   workspace?: string;
+  cwd?: string;
   agentDir?: string;
   /** Optional per-agent full system prompt replacement. */
   systemPromptOverride?: AgentDefaultsConfig["systemPromptOverride"];
diff --git a/src/config/zod-schema.agent-defaults.ts b/src/config/zod-schema.agent-defaults.ts
index c8e7486..c86c384 100644
--- a/src/config/zod-schema.agent-defaults.ts
+++ b/src/config/zod-schema.agent-defaults.ts
@@ -45,6 +45,7 @@ export const AgentDefaultsSchema = z
       )
       .optional(),
     workspace: z.string().optional(),
+    cwd: z.string().optional(),
     skills: z.array(z.string()).optional(),
     repoRoot: z.string().optional(),
     systemPromptOverride: z.string().optional(),
diff --git a/src/config/zod-schema.agent-runtime.ts b/src/config/zod-schema.agent-runtime.ts
index 2ee7cea..c4d394e 100644
--- a/src/config/zod-schema.agent-runtime.ts
+++ b/src/config/zod-schema.agent-runtime.ts
@@ -797,6 +797,7 @@ export const AgentEntrySchema = z
     default: z.boolean().optional(),
     name: z.string().optional(),
     workspace: z.string().optional(),
+    cwd: z.string().optional(),
     agentDir: z.string().optional(),
     systemPromptOverride: z.string().optional(),
     embeddedHarness: AgentEmbeddedHarnessSchema,
diff --git a/src/gateway/protocol/schema/logs-chat.ts b/src/gateway/protocol/schema/logs-chat.ts
index ff0ec48..64da9e1 100644
--- a/src/gateway/protocol/schema/logs-chat.ts
+++ b/src/gateway/protocol/schema/logs-chat.ts
@@ -36,6 +36,7 @@ export const ChatSendParamsSchema = Type.Object(
   {
     sessionKey: ChatSendSessionKeyString,
     message: Type.String(),
+    cwd: Type.Optional(Type.String()),
     thinking: Type.Optional(Type.String()),
     deliver: Type.Optional(Type.Boolean()),
     originatingChannel: Type.Optional(Type.String()),
diff --git a/src/gateway/server-methods/chat.ts b/src/gateway/server-methods/chat.ts
index 5f7bd49..56bd856 100644
--- a/src/gateway/server-methods/chat.ts
+++ b/src/gateway/server-methods/chat.ts
@@ -1763,6 +1763,7 @@ export const chatHandlers: GatewayRequestHandlers = {
     const p = params as {
       sessionKey: string;
       message: string;
+      cwd?: string;
       thinking?: string;
       deliver?: boolean;
       originatingChannel?: string;
@@ -2146,6 +2147,7 @@ export const chatHandlers: GatewayRequestHandlers = {
         dispatcher,
         replyOptions: {
           runId: clientRunId,
+          cwd: normalizeOptionalText(p.cwd),
           abortSignal: abortController.signal,
           images: parsedImages.length > 0 ? parsedImages : undefined,
           imageOrder: imageOrder.length > 0 ? imageOrder : undefined,
